<%= form_with(model: @mytweet, local:true) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="action">
    <%= form.submit %>
  </div>
<% end %>
